import { Simple } from "../ui";
export default function Page() { return <Simple title="Settings" body="Defaults are local to this prototype and never leave the browser."><div className="panel stack" style={{ maxWidth: 680, marginTop: 30 }}><label>Default target language<select className="field"><option>Spanish</option><option>English</option><option>French</option></select></label><label><input type="checkbox" defaultChecked /> Preserve document layout</label><label><input type="checkbox" defaultChecked /> Enable OCR when scanned pages are detected</label><label>OCR confidence threshold<input className="field" type="range" defaultValue="74" /></label><button className="btn" type="button">Save settings</button></div></Simple>; }
